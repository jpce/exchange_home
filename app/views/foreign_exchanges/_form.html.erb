<%= form_for(@foreign_exchange) do |f| %>
  <% if @foreign_exchange.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@foreign_exchange.errors.count, "error") %> prohibited this foreign_exchange from being saved:</h2>

      <ul>
      <% @foreign_exchange.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
